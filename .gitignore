node_modules/
dist/
.DS_Store

# large reference image from penrose-mosaic (kept locally, not tracked)
deca-shape-expansion.png

# generated net SVGs
out/

# vendored three.js (regenerate with: npm run vendor)
vendor/

# generated each build so the page can report which build it is running
src/build-id.ts
